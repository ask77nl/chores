<h1>Listing chores</h1>

<table>
  <tr>
    <th>Email</th>
    <th>Title</th>
    <th>Deadline</th>
    <th>Type</th>
    <th>Project</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @chores.each do |chore| %>
  <tr>
    <td><%= chore.email_id %></td>
    <td><%= chore.title %></td>
    <td><%= chore.deadline %></td>
    <td><%= chore.type %></td>
    <td><%= chore.project_id %></td>
    <td><%= link_to 'Show', chore %></td>
    <td><%= link_to 'Edit', edit_chore_path(chore) %></td>
    <td><%= link_to 'Destroy', chore, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Chore', new_chore_path %>

<%= form_for(@chore) do |f| %>
  <% if @chore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chore.errors.count, "error") %> prohibited this chore from being saved:</h2>

      <ul>
      <% @chore.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= collection_select(:chore, :email_id, Email.all, :id, :subject) %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :deadline %><br />
    <%= f.datetime_select :deadline %>
  </div>
  <div class="field">
    <%= collection_select(:chore, :choretype_id, Choretype.all, :id, :name) %>
  </div>
  <div class="field">
    <%= collection_select(:chore, :project_id, Project.all, :id, :title) %>
  </div>

  <div class="field">
    <%= collection_select(:chore, :user_id, User.all, :id, :email) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

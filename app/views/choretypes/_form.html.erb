<%= form_for(@choretype) do |f| %>
  <% if @choretype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@choretype.errors.count, "error") %> prohibited this choretype from being saved:</h2>

      <ul>
      <% @choretype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
